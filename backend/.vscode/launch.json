{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(OSX) Launch",
      "label": "debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/backend",
      "args": [],
      "cwd": "${workspaceRoot}"
    }
  ]
}
